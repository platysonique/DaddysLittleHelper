---
description: Superseded — use rzbrowse-browser-automation.mdc
alwaysApply: false
---

# Moved

Use [rzbrowse-browser-automation.mdc](./rzbrowse-browser-automation.mdc). Installer also copies that file to `~/.cursor/rules/` so it applies in every Cursor project on this machine.
